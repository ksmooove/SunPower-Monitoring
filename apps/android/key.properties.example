# Copy to apps/android/key.properties (gitignored) and fill in real values.
# Reuses the same upload keystore as other personal apps.

storeFile=C:/dev/SIGNKEYS/upload-keystore.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=upload
keyPassword=YOUR_KEY_PASSWORD
